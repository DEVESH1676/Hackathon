---
phase: 02-frontend-scaffolding
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [frontend-v2/src/layouts/MainLayout.tsx, frontend-v2/src/components/Navigation/PillNavbar.tsx, frontend-v2/src/App.tsx]
autonomous: true
requirements: [UI-02, UI-03]

must_haves:
  truths:
    - "Application displays a split view with Command Center and Health Pulse panels"
    - "Pill navbar is visible and tracks scroll position"
    - "Glassmorphism effects are applied to layout panels and navbar"
  artifacts:
    - path: "frontend-v2/src/layouts/MainLayout.tsx"
      provides: "Split view workspace structure"
    - path: "frontend-v2/src/components/Navigation/PillNavbar.tsx"
      provides: "Status-aware navigation"
  key_links:
    - from: "frontend-v2/src/App.tsx"
      to: "frontend-v2/src/layouts/MainLayout.tsx"
      via: "Component composition"
      pattern: "<MainLayout>"
---

<objective>
Implement the core UI shell, including the split-view layout and the status-aware pill navigation bar.

Purpose: Establish the structural "Elite Shell" that supports the intelligence feed and command console.
Output: A functional layout shell with responsive panels and navigation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-scaffolding/02-CONTEXT.md
@.planning/phases/02-frontend-scaffolding/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MainLayout (Split Workspace)</name>
  <files>frontend-v2/src/layouts/MainLayout.tsx</files>
  <action>
    Create a `MainLayout` component that implements the "Split Workspace" (per D-02, D-03).
    Left panel (Command Center): 60-70% width, contains submission form and logs.
    Right panel (Health Pulse / Intelligence Board): 30-40% width, contains system cards.
    Apply the "Glass" utility class to panels.
    Use `framer-motion` for basic entry animations.
  </action>
  <verify>
    <automated>grep "MainLayout" frontend-v2/src/layouts/MainLayout.tsx</automated>
  </verify>
  <done>Split-view layout implemented with glassmorphism styling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PillNavbar (Status-Aware)</name>
  <files>frontend-v2/src/components/Navigation/PillNavbar.tsx</files>
  <action>
    Create a `PillNavbar` component with "Status-Aware" behavior (per D-05).
    Include sections: "Classify", "Triage", "RAG", "Resolve", "Judge".
    Add support for:
    1. Scroll-to-section navigation.
    2. Active section tracking (highlighting current section in viewport).
    3. Pulse/Status indicators for each pill (mocked for now).
    Use a fixed position at the top or bottom of the screen.
  </action>
  <verify>
    <automated>grep "PillNavbar" frontend-v2/src/components/Navigation/PillNavbar.tsx</automated>
  </verify>
  <done>Pill navigation bar implemented with viewport tracking support.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble App Shell</name>
  <files>frontend-v2/src/App.tsx</files>
  <action>
    Update `App.tsx` to wrap the main content in `MainLayout`.
    Integrate `PillNavbar` into the layout.
    Create placeholder sections with matching IDs for the navbar to track.
  </action>
  <verify>
    <automated>grep "PillNavbar" frontend-v2/src/App.tsx</automated>
  </verify>
  <done>App entry point assembled with layout and navigation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Layout | UI components must handle overflow and responsiveness to prevent DOS (UI hanging) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | D | MainLayout | accept | Risk of layout break on extreme aspect ratios is acceptable for prototype |
</threat_model>

<verification>
Check visual layout in browser: Verify split panels and navbar are visible.
Click pills in navbar and verify scroll-to-section works for placeholders.
</verification>

<success_criteria>
- `MainLayout` with split workspace panels created.
- `PillNavbar` with scroll tracking functional.
- Glassmorphism styling visible on UI elements.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-scaffolding/02-02-SUMMARY.md`
</output>
