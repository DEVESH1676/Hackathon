---
phase: 02-frontend-scaffolding
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [frontend-v2/src/layouts/MainLayout.tsx, frontend-v2/src/components/Navigation/PillNavbar.tsx, frontend-v2/src/components/Submission/TicketForm.tsx, frontend-v2/src/App.tsx]
autonomous: true
requirements: [UI-02, UI-03]

must_haves:
  truths:
    - "Application displays a split view with Command Center and Health Pulse panels"
    - "Pill navbar tracks History and Analytics sections in addition to pipeline stages"
    - "Ticket submission form is present in the left Command Center panel"
    - "Placeholders for History and Analytics are visible below the fold"
  artifacts:
    - path: "frontend-v2/src/layouts/MainLayout.tsx"
      provides: "Split view workspace structure with placeholders for History/Analytics"
    - path: "frontend-v2/src/components/Navigation/PillNavbar.tsx"
      provides: "Status-aware navigation tracking stages + history + analytics"
    - path: "frontend-v2/src/components/Submission/TicketForm.tsx"
      provides: "Core ticket entry interface"
  key_links:
    - from: "frontend-v2/src/App.tsx"
      to: "frontend-v2/src/layouts/MainLayout.tsx"
      via: "Component composition"
      pattern: "<MainLayout>"
---

<objective>
Implement the core UI shell, including the split-view layout, the ticket submission form, and the status-aware pill navigation bar with extended section tracking.

Purpose: Establish the structural "Elite Shell" that supports the intelligence feed, command console, and long-term data surfaces.
Output: A functional layout shell with responsive panels, a submission form, and comprehensive navigation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-scaffolding/02-CONTEXT.md
@.planning/phases/02-frontend-scaffolding/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MainLayout (Split Workspace & Placeholders)</name>
  <files>frontend-v2/src/layouts/MainLayout.tsx</files>
  <action>
    Create a `MainLayout` component that implements the "Split Workspace" (per D-02, D-03).
    Above-the-fold:
    - Left panel (Command Center): 60-70% width, contains submission form and logs.
    - Right panel (Health Pulse / Intelligence Board): 30-40% width, contains system cards.
    Below-the-fold (per D-02, D-06):
    - Create placeholder sections for "History" and "Analytics" with distinct IDs.
    Apply the "Glass" utility class to panels.
    Use `framer-motion` for basic entry animations.
  </action>
  <verify>
    <automated>grep -E "History|Analytics" frontend-v2/src/layouts/MainLayout.tsx</automated>
  </verify>
  <done>Split-view layout implemented with glassmorphism styling and placeholders for History/Analytics.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PillNavbar (Status-Aware + History/Analytics)</name>
  <files>frontend-v2/src/components/Navigation/PillNavbar.tsx</files>
  <action>
    Create a `PillNavbar` component with "Status-Aware" behavior (per D-05).
    Include sections: "Classify", "Triage", "RAG", "Resolve", "Judge", and per feedback "History", "Analytics".
    Add support for:
    1. Scroll-to-section navigation.
    2. Active section tracking (highlighting current section in viewport).
    3. Pulse/Status indicators for each pill (mocked for now).
    Use a fixed position at the top or bottom of the screen.
  </action>
  <verify>
    <automated>grep -E "History|Analytics" frontend-v2/src/components/Navigation/PillNavbar.tsx</automated>
  </verify>
  <done>Pill navigation bar implemented with viewport tracking for all core sections.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Ticket Submission Form</name>
  <files>frontend-v2/src/components/Submission/TicketForm.tsx</files>
  <action>
    Implement the `TicketForm` component for the Left Panel (per D-03).
    Use Shadcn UI components: `Textarea` for ticket description, `Input` for metadata (optional), and a `Button` to submit.
    Add basic Zod validation for required fields.
    The form should trigger the pipeline run (mocked for now).
  </action>
  <verify>
    <automated>test -f frontend-v2/src/components/Submission/TicketForm.tsx</automated>
  </verify>
  <done>Ticket submission form implemented and ready for integration.</done>
</task>

<task type="auto">
  <name>Task 4: Assemble App Shell</name>
  <files>frontend-v2/src/App.tsx</files>
  <action>
    Update `App.tsx` to wrap the main content in `MainLayout`.
    Integrate `PillNavbar` into the layout.
    Integrate `TicketForm` into the left panel of `MainLayout`.
    Ensure the `MainLayout` renders the `TicketForm` in its Left Panel.
  </action>
  <verify>
    <automated>grep "TicketForm" frontend-v2/src/App.tsx || grep "TicketForm" frontend-v2/src/layouts/MainLayout.tsx</automated>
  </verify>
  <done>App entry point assembled with layout, navigation, and submission form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Layout | UI components must handle overflow and responsiveness to prevent DOS (UI hanging) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | D | MainLayout | accept | Risk of layout break on extreme aspect ratios is acceptable for prototype |
</threat_model>

<verification>
Check visual layout in browser: Verify split panels, navbar, and ticket form are visible.
Verify "History" and "Analytics" placeholders exist below the fold.
Click "History" in navbar and verify it scrolls to the placeholder.
</verification>

<success_criteria>
- `MainLayout` with split workspace panels and below-the-fold placeholders created.
- `PillNavbar` tracks all 7 sections (5 pipeline + History + Analytics).
- `TicketForm` correctly placed in the left panel per D-03.
- Glassmorphism styling visible on UI elements.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-scaffolding/02-02-SUMMARY.md`
</output>
