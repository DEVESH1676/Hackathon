---
phase: 02-frontend-scaffolding
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [frontend-v2/src/hooks/usePipeline.ts, frontend-v2/src/components/Pipeline/IntelligenceFeed.tsx, frontend-v2/src/components/Pipeline/StageCard.tsx]
autonomous: true
requirements: [UI-03]

must_haves:
  truths:
    - "Pipeline state correctly tracks multi-stage progress"
    - "Intelligence feed renders stages as they complete"
    - "SSE connection is explicitly closed on component unmount"
  artifacts:
    - path: "frontend-v2/src/hooks/usePipeline.ts"
      provides: "Pipeline state and SSE consumption logic with cleanup"
    - path: "frontend-v2/src/components/Pipeline/IntelligenceFeed.tsx"
      provides: "Vertical progressive disclosure container"
  key_links:
    - from: "frontend-v2/src/hooks/usePipeline.ts"
      to: "frontend-v2/src/components/Pipeline/IntelligenceFeed.tsx"
      via: "State subscription"
      pattern: "usePipeline"
---

<objective>
Establish the reactive pipeline state layer and implement the progressive disclosure intelligence feed, utilizing type-safe contracts from the backend and ensuring robust resource management.

Purpose: Create the "Nerve Center" that orchestrates the 5-stage agentic workflow in the UI.
Output: A functional pipeline hook and feed components capable of consuming streaming data with proper cleanup.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-scaffolding/02-CONTEXT.md
@.planning/phases/02-frontend-scaffolding/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement usePipeline Hook with SSE Cleanup</name>
  <files>frontend-v2/src/hooks/usePipeline.ts</files>
  <action>
    Create a `usePipeline` hook using `useReducer` to manage pipeline state (per D-07).
    State should track: `status` (idle, running, completed), `progress` (0.0-1.0), `stages` (Map of stage ID to data), and `logs` (array of messages).
    Implement `EventSource` consumption logic for SSE (per D-04).
    CRITICAL: Ensure the `EventSource` is closed explicitly in the `useEffect` cleanup function (`es.close()`) to prevent memory leaks and multiple active connections.
    Use generated TypeScript types from `src/types/api.ts` for pipeline payloads (per D-10).
    Include a "mock mode" that simulates a pipeline run with timed updates (per D-06).
  </action>
  <verify>
    <automated>grep "es.close()" frontend-v2/src/hooks/usePipeline.ts</automated>
  </verify>
  <done>Pipeline state hook implemented with SSE, robust cleanup, mock support, and type safety.</done>
</task>

<task type="auto">
  <name>Task 2: Implement IntelligenceFeed & StageCards</name>
  <files>frontend-v2/src/components/Pipeline/IntelligenceFeed.tsx, frontend-v2/src/components/Pipeline/StageCard.tsx</files>
  <action>
    Create an `IntelligenceFeed` component that maps through the `stages` in pipeline state.
    Create a `StageCard` component for each stage (Classify, Triage, RAG, Resolve, Judge).
    Cards should animate into view (Progressive Disclosure) as their stage starts/completes.
    Use `framer-motion` for reveal transitions.
  </action>
  <verify>
    <automated>grep "IntelligenceFeed" frontend-v2/src/components/Pipeline/IntelligenceFeed.tsx</automated>
  </verify>
  <done>Intelligence feed components implemented with animation support.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Feed into MainLayout</name>
  <files>frontend-v2/src/layouts/MainLayout.tsx</files>
  <action>
    Integrate the `IntelligenceFeed` into the right panel of `MainLayout`.
    Connect the `TicketForm` (from Plan 02) to trigger the `usePipeline` hook.
    Ensure the `PillNavbar` correctly tracks the `IntelligenceFeed` sections as they appear.
  </action>
  <verify>
    <automated>grep "IntelligenceFeed" frontend-v2/src/layouts/MainLayout.tsx</automated>
  </verify>
  <done>Pipeline feed integrated into the main application layout and wired to the submission form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE Stream → State | Malformed SSE events could crash the reducer or cause infinite loops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | D | usePipeline | mitigate | Implement defensive parsing of SSE payloads with try-catch blocks and type validation |
</threat_model>

<verification>
Click "Submit" in the Ticket Form and verify:
1. SSE connection starts (check Network tab).
2. Stages appear one by one in the Intelligence Feed.
3. Progress bar updates.
4. Navbar highlights active section as cards animate.
5. Close the tab or unmount component and verify `EventSource` is closed.
</verification>

<success_criteria>
- `usePipeline` hook manages complex multi-stage state and handles SSE cleanup.
- `IntelligenceFeed` renders mocked/real stage results progressively.
- Visual feedback (progress/status) matches pipeline state.
- Hook uses generated API types for payload structures.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-scaffolding/02-03-SUMMARY.md`
</output>
