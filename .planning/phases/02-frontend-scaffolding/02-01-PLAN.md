---
phase: 02-frontend-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend-v2/package.json, frontend-v2/tailwind.config.ts, frontend-v2/src/index.css, frontend-v2/components.json]
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Vite dev server starts and serves a React application"
    - "Tailwind CSS is functional with custom glassmorphism utilities"
    - "Shadcn UI is initialized and ready for component installation"
  artifacts:
    - path: "frontend-v2/package.json"
      provides: "Project dependencies and scripts"
    - path: "frontend-v2/tailwind.config.ts"
      provides: "Design tokens and Glassmorphism plugin"
    - path: "frontend-v2/components.json"
      provides: "Shadcn UI configuration"
  key_links:
    - from: "frontend-v2/src/main.tsx"
      to: "frontend-v2/src/App.tsx"
      via: "React rendering"
      pattern: "ReactDOM.createRoot"
---

<objective>
Initialize the frontend project foundation using Vite, React, and Tailwind CSS, and configure the core design system for Glassmorphism.

Purpose: Establish the modern UI architecture and visual primitives required for the Nexus AI "Elite Shell".
Output: A functional Vite project with Tailwind, Shadcn, and basic styling tokens configured.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-scaffolding/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React (TypeScript) project</name>
  <files>frontend-v2/package.json, frontend-v2/vite.config.ts, frontend-v2/tsconfig.json</files>
  <action>
    Initialize a new Vite project in the `frontend-v2` directory using the `react-ts` template.
    Install core dependencies: react, react-dom, framer-motion, lucide-react, clsx, tailwind-merge.
    Configure `vite.config.ts` to include a proxy for `/api` pointing to `http://localhost:8000` (per D-10).
  </action>
  <verify>
    <automated>cd frontend-v2 && npm list react vite</automated>
  </verify>
  <done>Vite project structure created with TypeScript support and API proxy configured.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS and Glassmorphism Plugin</name>
  <files>frontend-v2/tailwind.config.ts, frontend-v2/src/index.css, frontend-v2/postcss.config.js</files>
  <action>
    Initialize Tailwind CSS in the project.
    Update `tailwind.config.ts` to include a custom plugin or utility for the "Glass" material (per D-08):
    `backdrop-filter: blur(20px)`, `background: rgba(255, 255, 255, 0.03)`, `border: 1px solid rgba(255, 255, 255, 0.1)`.
    Define custom animations for `aurora` and `gradient-xy` (per D-08, D-09).
    Add standard Shadcn color variables to `index.css`.
  </action>
  <verify>
    <automated>grep "backdrop-filter" frontend-v2/tailwind.config.ts || grep "glass" frontend-v2/src/index.css</automated>
  </verify>
  <done>Tailwind configured with Glassmorphism primitives and required animations.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize Shadcn UI</name>
  <files>frontend-v2/components.json, frontend-v2/src/lib/utils.ts</files>
  <action>
    Initialize Shadcn UI using the CLI: `npx shadcn-ui@latest init`.
    Select "Stone" or "Slate" base color, "New York" style, and enable CSS variables.
    Ensure `src/lib/utils.ts` is created with the `cn` helper.
  </action>
  <verify>
    <automated>test -f frontend-v2/components.json && test -f frontend-v2/src/lib/utils.ts</automated>
  </verify>
  <done>Shadcn UI initialized and ready for component usage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Untrusted input from user via forms, streaming response from backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S | API Proxy | mitigate | Ensure Vite proxy only targets the local backend port (8000) |
| T-02-02 | I | Form Inputs | mitigate | Use standard Shadcn Input components; validation to be added in later tasks |
</threat_model>

<verification>
Run `npm run dev` in `frontend-v2` and verify the Vite welcome page loads.
Verify `tailwind.config.ts` contains the glassmorphism definitions.
</verification>

<success_criteria>
- Vite project initialized in `frontend-v2/`.
- Tailwind CSS functional with custom "glass" class.
- Shadcn UI initialized with `components.json`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-scaffolding/02-01-SUMMARY.md`
</output>
